# PRD — 쓰담하개

## 1. 문서 정보

- 제품명: 쓰담하개
- 제품명(영어): PuPat
- 문서 버전: v0.1
- 작성일: 2026-08-26
- 제품 유형: 반응형 웹 인터랙션 서비스 → PWA → 향후 Desktop App 확장
- 핵심 가치: 강아지를 키울 수 없는 사람도 웹에서 부담 없이 강아지를 쓰다듬는 경험을 제공한다.

---

## 2. 제품 개요

### 2.1 배경

현실에서는 반려견을 키우기 어렵지만 강아지를 쓰다듬고 싶어 하는 사람이 있다. 이 서비스는 복잡한 기능이나 콘텐츠를 제공하기보다, 사용자가 웹페이지에 들어와 강아지를 보고 직접 마우스로 쓰다듬으며 잠시 편안함을 느낄 수 있는 짧은 인터랙션 경험을 제공하는 것을 목표로 한다.

### 2.2 핵심 컨셉

> "강아지를 키울 수는 없어도, 쓰다듬을 수는 있으니까."

사용자는 화면 속 강아지에게 마우스를 움직이고 클릭/드래그하는 방식으로 상호작용한다. 강아지는 사용자의 행동에 따라 고개, 눈, 귀, 꼬리, 표정 등을 변화시키며 반응한다.

### 2.3 제품 목표

1. 사용자가 별도의 학습 없이 5초 이내에 강아지를 만져볼 수 있게 한다.
2. 단순 클릭 애니메이션이 아니라 실제로 강아지를 쓰다듬는 듯한 인터랙션을 제공한다.
3. 짧은 시간 동안 반복해서 쓰다듬고 싶을 정도의 감각적 경험을 만든다.
4. 초기 버전은 무료 기술과 오픈소스 도구를 활용하여 개인 개발자가 구축할 수 있는 규모로 제한한다.

---

## 3. 타깃 사용자

### Primary Persona

- 강아지를 좋아하지만 현재 반려견을 키우지 않는 사람
- 잠깐 쉬면서 귀여운 콘텐츠를 보고 싶은 사람
- 마우스나 터치로 캐릭터를 직접 조작하는 인터랙션을 좋아하는 사람
- 짧은 시간 동안 기분 전환을 원하는 사람

### 사용 상황

- 공부나 업무 중 잠깐 쉬는 시간
- 스트레스를 받았을 때
- 자기 전
- 심심할 때
- SNS나 커뮤니티에서 링크를 보고 가볍게 방문했을 때

---

## 4. 핵심 사용자 경험

### 4.1 기본 플로우

```text
웹사이트 접속
    ↓
강아지가 사용자를 바라봄
    ↓
사용자가 마우스를 움직임
    ↓
강아지가 마우스 방향으로 고개/시선을 움직임
    ↓
사용자가 강아지를 클릭하고 드래그
    ↓
쓰다듬기 판정
    ↓
강아지가 눈을 감거나 귀/꼬리/몸을 움직임
    ↓
사용자가 계속 쓰다듬음
    ↓
강아지가 행복한 상태로 변화
```

### 4.2 핵심 경험 원칙

- 설명보다 강아지의 행동으로 사용법을 알려준다.
- 입력에 대한 반응은 가능한 한 즉각적이어야 한다.
- 애니메이션은 과장하기보다 자연스럽고 부드럽게 만든다.
- 기능 수보다 인터랙션의 완성도를 우선한다.
- 사용자가 "내 행동에 강아지가 반응한다"고 느끼게 한다.

---

## 5. MVP 범위

### 5.1 MVP에 반드시 포함

#### 강아지 캐릭터

- 1종의 강아지
- 파츠가 분리된 일러스트
- 머리
- 양쪽 귀
- 양쪽 눈
- 코
- 입
- 몸
- 꼬리

#### 마우스 추적

- 마우스 위치 감지
- 강아지가 마우스 방향을 바라봄
- 좌/우 및 상/하 방향에 따른 고개 움직임
- 움직임에 자연스러운 easing 적용

#### 쓰다듬기

- 마우스 버튼을 누른 상태에서 드래그
- 강아지의 쓰다듬기 영역 감지
- 일정 거리 이상 움직이면 쓰다듬기 행동으로 판정
- 쓰다듬는 동안 애니메이션 재생

#### 기본 반응

- 눈 감기
- 귀 움직임
- 꼬리 흔들기
- 머리 움직임
- 행복한 표정
- idle 상태

#### 기본 UI

- 중앙에 강아지 표시
- 최소한의 안내 문구
- 쓰다듬기 상태를 방해하지 않는 배경
- 모바일/데스크톱 기본 반응형 대응

---

## 6. 강아지 상태 머신

초기에는 다음 상태를 사용한다.

```text
IDLE
  │
  ├── 마우스 이동 ──→ LOOKING
  │
  ├── 일정 시간 무입력 ──→ SLEEPY
  │
  └── 쓰다듬기 시작 ──→ PETTING

LOOKING
  │
  ├── 쓰다듬기 ──→ PETTING
  └── 일정 시간 무입력 ──→ IDLE

PETTING
  │
  ├── 일정 시간 이상 쓰다듬음 ──→ HAPPY
  └── 입력 중단 ──→ IDLE

HAPPY
  │
  └── 입력 중단 ──→ IDLE

SLEEPY
  │
  └── 마우스 움직임 ──→ LOOKING
```

상태 머신은 MVP에서 명확하게 분리하여 이후 반응 종류를 추가하기 쉽게 설계한다.

---

## 7. 인터랙션 상세

### 7.1 마우스 추적

사용자의 마우스 위치를 강아지 기준 상대 좌표로 변환한다.

예:

```text
마우스가 오른쪽
→ 강아지 머리 오른쪽 회전
→ 오른쪽 눈/시선 이동

마우스가 왼쪽
→ 강아지 머리 왼쪽 회전

마우스가 위쪽
→ 고개를 살짝 위로

마우스가 아래쪽
→ 고개를 살짝 아래로
```

회전 각도는 제한하여 캐릭터가 부자연스럽게 꺾이지 않도록 한다.

### 7.2 쓰다듬기 판정

사용자가 강아지의 쓰다듬기 영역에서 마우스를 누른 뒤 움직이는 경우 PETTING 상태로 전환한다.

고려할 입력값:

- pointerdown
- pointermove
- pointerup
- 이동 거리
- 이동 방향
- 이동 속도
- 마지막 입력 시간
- 쓰다듬기 영역

초기 MVP에서는 복잡한 물리 계산을 피하고 "누른 상태 + 일정 거리 이상 이동"을 기본 조건으로 사용한다.

### 7.3 쓰다듬기 영역

MVP에서는 머리와 몸을 주요 쓰다듬기 영역으로 지정한다.

향후 다음 영역으로 확장한다.

```text
귀
머리
턱
목
등
몸
꼬리
발
```

각 영역에 서로 다른 반응을 추가할 수 있도록 구조를 확장 가능하게 설계한다.

---

## 8. 애니메이션 설계

### 8.1 캐릭터 파츠

```text
Dog
├── Body
├── Head
├── LeftEar
├── RightEar
├── LeftEye
├── RightEye
├── Nose
├── Mouth
├── Tail
└── Paw
```

### 8.2 MVP 애니메이션

| 애니메이션 | 트리거 |
|---|---|
| Idle breathing | 기본 상태 |
| Head tracking | 마우스 이동 |
| Eye blink | 랜덤/idle |
| Happy eyes | 쓰다듬기 |
| Ear movement | 쓰다듬기 |
| Tail wag | 일정 시간 쓰다듬기 |
| Head tilt | 특정 방향 입력 |
| Sleep | 장시간 무입력 |

### 8.3 구현 방향

초기에는 Live2D를 직접 도입하기보다 SVG/CSS/JavaScript 기반 파츠 애니메이션을 우선한다.

이유:

- 구현 난이도가 낮음
- 웹 배포가 쉬움
- 바이브코딩으로 빠르게 프로토타이핑 가능
- 캐릭터 상태와 애니메이션을 코드로 직접 제어하기 쉬움

향후 더 높은 수준의 캐릭터 움직임이 필요하면 Live2D 도입을 검토한다.

---

## 9. UX/UI

### 9.1 메인 화면

서비스의 핵심은 강아지이므로 UI를 최소화한다.

```text
┌──────────────────────────────────┐
│                                  │
│                                  │
│               🐶                 │
│                                  │
│        마우스로 쓰다듬어 주세요    │
│                                  │
│                                  │
│       오늘의 쓰담 02:31            │
└──────────────────────────────────┘
```

### 9.2 디자인 원칙

- 강아지가 화면의 주인공
- 넓은 여백
- 부드러운 애니메이션
- 시각적 요소 최소화
- 강아지와 상호작용하는 영역을 명확하게 구분
- 모바일에서는 터치 인터랙션을 고려

---

## 10. 사운드

MVP에서는 선택 기능으로 둔다.

가능한 사운드:

- 작은 숨소리
- 꼬리 흔드는 소리
- 부드러운 낑낑거림
- 기분 좋은 강아지 소리
- 배경 ambience

사용자가 원하지 않을 수 있으므로 음소거 기능을 제공한다.

사운드는 저작권 문제가 없는 무료/직접 제작 리소스를 우선 사용한다.

---

## 11. 기술 요구사항

### 권장 기술 스택

- Framework: Next.js
- Language: TypeScript
- Styling: CSS / Tailwind CSS
- Character: SVG
- Animation: CSS Animation / Framer Motion 또는 유사 라이브러리
- Deployment: Vercel
- Version Control: GitHub

### 기술 원칙

- 서버가 없어도 핵심 기능이 동작하도록 설계
- 초기에는 DB와 로그인 기능을 사용하지 않는다.
- 무료 배포 환경을 우선한다.
- 클라이언트에서 인터랙션을 처리한다.
- 모바일 터치 이벤트를 고려하여 Pointer Events 기반으로 구현한다.
- Desktop Web / Mobile Web / PWA를 별도의 애플리케이션이나 페이지로 분리하지 않고 하나의 Next.js + React 코드베이스에서 구현한다.
- 모바일 전용 UI를 별도 페이지로 만들지 않고 반응형 레이아웃으로 구현한다.
- PWA는 기존 웹 앱에 Web App Manifest, Service Worker, 설치/standalone 실행 등의 기능을 추가하는 방식으로 확장한다.
- 강아지의 상태, 행동, 인터랙션 로직과 UI 컴포넌트를 분리하여 향후 플랫폼을 확장할 수 있도록 한다.
- 마우스와 터치 입력은 가능한 한 공통 Pointer Events 기반 인터랙션 시스템으로 처리한다.
- 향후 Tauri 기반 Desktop App으로 확장할 수 있도록 브라우저 전용 기능에 핵심 로직이 강하게 종속되지 않도록 한다.

---

## 12. 플랫폼 확장 전략

쓰담하개는 웹에서 시작하여 PWA를 거쳐 향후 Desktop App으로 확장한다. 각 플랫폼을 별도의 서비스로 개발하지 않고, 가능한 한 동일한 React UI와 핵심 인터랙션 로직을 재사용한다.

### 12.1 전체 구조

```text
                    Next.js + React
                          │
             ┌────────────┼────────────┐
             ↓            ↓            ↓
        Desktop Web   Mobile Web      PWA

                          │
                          ↓
                  향후 Tauri Desktop
```

현재 MVP에서는 Desktop App을 구현하지 않는다. 우선 완성도 높은 반응형 웹을 만든 뒤 PWA를 추가하고, 이후 필요성이 확인되면 Tauri 기반 Desktop App으로 확장한다.

### 12.2 반응형 웹

- Desktop / Tablet / Mobile을 하나의 애플리케이션에서 지원한다.
- 별도의 `/mobile` 같은 모바일 전용 페이지를 만들지 않는다.
- Tailwind CSS 또는 CSS media query를 사용하여 화면 크기에 따라 레이아웃과 인터랙션 영역을 조정한다.
- 디자인 시안의 Desktop / Mobile 화면이 서로 다른 경우에도 가능한 한 동일한 컴포넌트를 재사용한다.
- 화면 크기에 따라 강아지 크기, 여백, 텍스트, 버튼, 인터랙션 영역 등을 자연스럽게 조정한다.

### 12.3 PWA

PWA는 별도의 모바일 앱을 만드는 것이 아니라 기존 반응형 웹 앱을 설치 가능한 앱 형태로 확장한다.

```text
Mobile Browser
      ↓
반응형 Mobile UI
      ↓
홈 화면에 설치
      ↓
PWA
      ↓
동일한 React UI / 동일한 인터랙션 로직
```

PWA에서 다음 기능을 단계적으로 적용할 수 있도록 구조를 마련한다.

- Web App Manifest
- Service Worker
- 앱 아이콘
- standalone display mode
- 기본적인 오프라인 캐시

PWA 전용 UI를 별도로 만들지 않는다.

### 12.4 Desktop App

향후 Tauri를 사용하여 Desktop App으로 확장하는 것을 고려한다.

목표는 단순히 웹사이트를 데스크톱 창으로 띄우는 것이 아니라 다음과 같은 데스크톱 경험을 제공하는 것이다.

- 바탕화면 위에 강아지가 상주
- 다른 프로그램 위에서도 강아지가 보이는 창
- 투명한 창
- 창 위치 이동
- 마우스 기반 쓰다듬기
- 컴퓨터 시작 시 실행
- Desktop 전용 알림 및 상호작용

현재 MVP에서는 Tauri/Rust를 도입하지 않는다. 다만 향후 Tauri WebView에서 기존 React UI를 최대한 재사용할 수 있도록 웹 애플리케이션과 강아지 핵심 로직을 구조적으로 분리한다.

### 12.5 강아지 로직과 UI 분리

강아지의 상태와 행동은 UI 컴포넌트에 과도하게 하드코딩하지 않는다.

권장 개념:

```text
사용자 입력
    ↓
Interaction
    ↓
Dog State
    ↓
Dog Behavior
    ↓
Animation / UI
```

강아지 상태 예시:

```text
mood
affection
energy
attention
currentAction
```

인터랙션 예시:

```text
pet
pointer move
pointer down
pointer up
drag
hover
idle
```

행동 예시:

```text
idle
looking
petting
happy
sleepy
```

이를 통해 웹, PWA, 향후 Desktop App에서 강아지의 핵심 행동 로직을 재사용할 수 있도록 한다.

### 12.6 입력 방식

Desktop에서는 마우스, Mobile에서는 터치를 사용하지만 이를 별도의 인터랙션 시스템으로 분리하지 않는다.

가능한 경우 다음과 같이 공통 처리한다.

```text
Mouse ─────┐
           ├──→ Pointer Events ──→ 공통 인터랙션 로직
Touch ─────┘
```

Desktop에서는 hover와 세밀한 마우스 이동을 활용하고, Mobile에서는 충분한 터치 영역과 drag/swipe 등의 입력을 활용한다.

### 12.7 확장성을 위한 컴포넌트 구조

프로젝트 규모에 따라 조정할 수 있지만 다음과 같은 관심사 분리를 권장한다.

```text
components/
├── dog/
│   ├── Dog.tsx
│   ├── DogAnimation.tsx
│   ├── DogInteraction.tsx
│   └── DogStatus.tsx
│
├── ui/
└── layout/

lib/
├── dog/
│   ├── dogState.ts
│   ├── dogBehavior.ts
│   └── dogInteraction.ts
└── ...
```

핵심은 정확한 폴더 이름보다 UI, 상태, 인터랙션, 비즈니스 로직이 하나의 파일에 과도하게 결합되지 않는 것이다.

---

## 13. 비기능 요구사항

### 성능

- 초기 로딩이 빠를 것
- 캐릭터 애니메이션이 끊기지 않을 것
- 불필요한 React re-render를 최소화할 것
- 마우스 이벤트 처리로 프레임 드랍이 발생하지 않도록 할 것

### 접근성

- 마우스를 사용할 수 없는 환경에서도 기본적인 콘텐츠 확인 가능
- 애니메이션을 줄이는 `prefers-reduced-motion` 대응 검토
- 사운드는 기본적으로 사용자가 제어할 수 있도록 함

### 반응형

- Desktop 우선
- 모바일/태블릿에서 터치로 쓰다듬을 수 있도록 확장
- 다양한 화면 크기에서 강아지가 잘리지 않도록 처리

---

## 14. MVP 제외 범위

초기 버전에서는 다음 기능을 구현하지 않는다.

- 회원가입/로그인
- 서버/DB
- 강아지 육성 시스템
- 결제
- 커뮤니티
- 채팅
- 강아지 20종 이상 제공
- 복잡한 게임 시스템
- 실제 스트레스 측정
- 의료적/심리적 효과를 측정하거나 보장하는 기능

특히 "스트레스가 실제로 감소한다"는 것을 서비스가 측정하거나 의학적으로 주장하지 않는다.

---

## 15. 향후 확장 아이디어

### Phase 2 — 강아지 반응 강화

- 부위별 상호작용
- 쓰다듬기 속도에 따른 반응
- 쓰다듬기 방향에 따른 반응
- 강아지별 성격
- 다양한 idle animation
- 랜덤 행동

### Phase 3 — 여러 강아지

- 강아지 종류 선택
- 강아지 이름 설정
- 강아지별 반응 차이
- 시바견 / 골든리트리버 / 푸들 / 말티즈 등

### Phase 4 — 개인화

- 오늘의 쓰담 시간
- 방문 횟수
- 누적 쓰담 횟수
- 가장 많이 쓰다듬은 강아지
- 일일 기록
- 로컬 스토리지 기반 저장

### Phase 5 — 공유

- 오늘의 쓰담 기록 이미지 생성
- 강아지 링크 공유
- "내 강아지 보여주기"
- SNS 공유

---

## 16. 성공 지표

초기에는 사업적 KPI보다 인터랙션 품질을 측정한다.

### 핵심 지표

- 첫 방문 후 강아지를 실제로 쓰다듬은 비율
- 첫 인터랙션까지 걸린 시간
- 평균 쓰다듬기 지속 시간
- 한 세션에서 쓰다듬기 횟수
- 재방문율

### 가장 중요한 지표

> **사용자가 처음 접속한 뒤 실제로 강아지를 쓰다듬는가?**

서비스의 핵심 가치가 "쓰다듬는 경험"이기 때문에 페이지뷰나 체류시간만으로 성공 여부를 판단하지 않는다.

---

## 17. 개발 우선순위

### Priority 1 — 핵심 인터랙션

1. 강아지 SVG 제작
2. 강아지 파츠 분리
3. 마우스 위치 추적
4. 고개 방향 제어
5. Pointer Events 처리
6. 쓰다듬기 영역 판정
7. 쓰다듬기 애니메이션

### Priority 2 — 생명감

8. 눈 깜빡임
9. 꼬리 흔들기
10. 귀 움직임
11. Idle breathing
12. Sleepy 상태
13. Happy 상태

### Priority 3 — 완성도

14. 사운드
15. 반응형
16. reduced-motion
17. 모바일 터치 대응
18. 성능 최적화
19. 배포

### Priority 4 — 확장

20. 부위별 반응
21. 여러 강아지
22. 쓰담 기록
23. 공유 기능

---

## 18. 핵심 개발 원칙

이 프로젝트에서 가장 중요한 것은 기능의 개수가 아니다.

### 반드시 지킬 것

1. **강아지 한 마리를 제대로 만든다.**
2. **사용자의 입력에 즉각적으로 반응한다.**
3. **애니메이션의 자연스러움을 우선한다.**
4. **UI보다 캐릭터 인터랙션을 우선한다.**
5. **MVP에서는 서버와 로그인 등을 만들지 않는다.**
6. **"귀엽다"보다 "내가 만지고 있다"는 느낌을 목표로 한다.**

---

## 19. MVP 완료 기준

다음 조건을 모두 만족하면 MVP 완료로 판단한다.

- [ ] 사용자가 페이지에 접속할 수 있다.
- [ ] 강아지가 화면에 자연스럽게 표시된다.
- [ ] 마우스 위치에 따라 강아지가 사용자를 바라본다.
- [ ] 사용자가 강아지를 누르고 드래그하면 쓰다듬기가 인식된다.
- [ ] 쓰다듬는 동안 강아지가 눈/귀/꼬리 등으로 반응한다.
- [ ] 쓰다듬기를 멈추면 자연스럽게 기본 상태로 돌아간다.
- [ ] 일정 시간 입력이 없으면 강아지가 idle/sleep 상태로 전환된다.
- [ ] 모바일에서도 기본적인 터치 인터랙션이 가능하다.
- [ ] Mobile Web과 PWA에서 동일한 핵심 UI와 인터랙션을 사용할 수 있는 구조다.
- [ ] 모바일 전용 페이지를 별도로 만들지 않고 반응형으로 동작한다.
- [ ] 마우스와 터치 입력이 공통 Pointer Events 기반으로 처리된다.
- [ ] 강아지 상태/행동/인터랙션 로직이 UI 컴포넌트와 분리되어 있다.
- [ ] 향후 Tauri Desktop App에서 핵심 UI와 강아지 로직을 재사용할 수 있는 구조다.
- [ ] 데스크톱에서 애니메이션이 부드럽게 동작한다.
- [ ] Vercel 등 무료 환경에 배포할 수 있다.

---

## 20. 한 문장 정의

> **쓰담하개는 강아지를 키울 수 없는 사람이 웹에서 마우스로 강아지를 직접 쓰다듬으며 잠깐 쉬어갈 수 있는 인터랙티브 웹 서비스다.**
